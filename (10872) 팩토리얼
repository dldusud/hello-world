#include <stdio.h>

int fac(int N)
{
    if(N > 1)
        return N * fac(N-1);
    else if(N == 0)
        return 1;
    else
        return N;
}

int main()
{
    int N = 0;
    scanf("%d",&N);
    
    printf("%d\n", fac(N));
    
    return 0;
}
