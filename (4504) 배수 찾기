#include <stdio.h>

int main()
{
	int n, num1;
	scanf("%d\n%d", &n, &num1);

	while (num1 != 0)
	{
		if (num1 % n == 0)
			printf("%d is a multiple of %d.\n", num1, n);
		else
			printf("%d is NOT a multiple of %d.\n", num1, n);
		scanf("%d", &num1);
	}

	return 0;

}
