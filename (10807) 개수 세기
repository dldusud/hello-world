#include <stdio.h>

int main()
{
	int N = 0;
	scanf("%d", &N);

	int arr[101];

	for (int i = 0; i < N; i++)
		scanf("%d ", &arr[i]);

	int v = 0;
	scanf("%d", &v);

	int count = 0;
	for (int i = 0; i < N; i++)
	{
		if (arr[i] == v)
			count++;
	}

	printf("%d\n", count);

	return 0;
}
