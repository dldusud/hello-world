#include <stdio.h>

int main() 
{
	int T;
	scanf("%d", &T);

	for (int i = 1; i <= T; i++)
	{
		int A, B, C;

		scanf("%d %d", &A, &B);

		C = A + B;

		printf("Case #%d: %d + %d = %d\n", i, A, B, C);
	}

	return 0;
}
